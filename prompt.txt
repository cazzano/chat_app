now i have an ui problem that when i send one message then the cursor removed from the input box of message to send message and i want that when ever i press enter then it will send the message but it will stay at the message input box okay and the other issue was that the scroll is not automatically doing down cuz when messages are in large quantity it should automatically scroll down haah, you can see the errors in the picture okay.
