now add an effect that when i send the message then it will mimic the whatsapp like logic that it will stay in the input message box and always focuses on it and in android/ios it will use the send button inside the keyboard button but in windows/linux/macos it will send the message by enter and it will always focuses in the message input box and send the message then the bubble will start sending message individually haah shows pending when sending and shows single tick when sent haah and it will instantly send the message to the bubble and the next work is to done by the bubble haah like in whatsapp.
